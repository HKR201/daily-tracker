name: daily_tracker
description: A new Flutter project for Daily Expense Tracking.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.1.1
  sqflite: ^2.3.0
  path_provider: ^2.1.2
  intl: ^0.19.0
  shared_preferences: ^2.2.2
  math_expressions: ^2.4.0
  google_sign_in: ^6.2.1
  googleapis: ^13.1.0
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
